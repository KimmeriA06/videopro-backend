services:
  - type: web
    name: videopro-backend
    env: python
    plan: free
    runtime: python
    buildCommand: pip install -r gereksinimler.txt
    startCommand: uvicorn ana:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.8"
